/sdd-emb:tasks batch-notify --depth=easy

Headless eval run — there is NO interactive user; never call AskUserQuestion. Follow the tasks
skill's protocol exactly as written; use `<TBD lead>` as every owner.
