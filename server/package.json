{
  "name": "sdd-emb-dashboard",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "test": "bun test tests/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.2.0",
    "typescript": "^5.5.0"
  }
}
