# Plugin-local settings (per-project, auto-created with documented defaults by the pipeline —
# normally `specify` at the backbone start, or `implement`). The glob covers sdd-emb.local.md.
.claude/*.local.md

# Implementation engine worktrees
.worktrees/

# Dashboard MCP server — installed deps (bun install runs on first launch; bun.lock is committed)
server/node_modules/

# Playwright / browser-automation scratch output
.playwright-mcp/
server/dash.png

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/
